{
  "name": "betamax",
  "private": true,
  "description": "Static BetaMax home page — hero section and navigation bar, no build step.",
  "scripts": {
    "test": "node --test tests/page.test.mjs"
  }
}
