{
  "name": "alpha-centuri",
  "private": true,
  "description": "Static Alpha Centuri home page — hero section and navigation bar, no build step.",
  "scripts": {
    "test": "node --test tests/page.test.mjs"
  }
}
